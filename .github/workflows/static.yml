# Simple workflow for deploying a Jaspr static site to Pages
name: Deploy static content to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Setup Dart
        uses: dart-lang/setup-dart@v1.3

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: dart pub get

      - name: Install Tailwind CSS
        run: |
          curl -sLO https://github.com/tailwindlabs/tailwindcss/releases/latest/download/tailwindcss-linux-x64
          chmod +x tailwindcss-linux-x64
          sudo mv tailwindcss-linux-x64 /usr/local/bin/tailwindcss

      - name: Build CSS
        run: |
          tailwindcss -i web/styles.tw.css -o web/styles.css --minify --config tailwind.config.js
          echo "CSS file size:"
          ls -lh web/styles.css

      - name: Build Jaspr
        run: |
          dart pub global activate jaspr_cli
          jaspr build --verbose --dart-define=BASE_PATH=/portfolio/
          ls -la build/
          echo "Build/jaspr contents:"
          ls -la build/jaspr/
          echo "Check if styles.css exists in build:"
          test -f build/jaspr/styles.css && echo "styles.css found" || echo "styles.css NOT found"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'build/jaspr'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
